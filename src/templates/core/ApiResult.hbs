{{>header}}

export interface ApiResult<T> {
	readonly url: string;
	readonly type: 'success' | 'failure';
	readonly httpCode: number;
	readonly httpCodeText: string;
	readonly responseHeaders: Headers;
	readonly body: T | undefined;
	readonly error: any | undefined;
};
